# random module

# randint
# 두 수 사이의 어떤 랜덤한 정수를 리턴

import random

number = random.randint(1, 20))
print(number)



# uniform
# 두 수 사이의 랜덤한 소수를 리턴

import uniform

decimal = random.uniform(0, 1)
print(decimal)
